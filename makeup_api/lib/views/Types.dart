// ignore_for_file: file_names, use_key_in_widget_constructors
/*
import 'package:flutter/material.dart';
import 'package:get/get.dart';
import 'package:makeup_api/controllers/prodcutController.dart';
import 'package:makeup_api/models/productModel.dart';

class Types extends StatelessWidget {
 final ProductType type;
 int index ;

  String imge;
  // ProductController controller=Get.put(ProductController());

  Types(this.type, this.imge,this ind);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Container(
          decoration: BoxDecoration(
              borderRadius: BorderRadius.circular(20),
              color: Color.fromARGB(255, 139, 116, 116)),
          height: 60,
          width: 61,
          child: Padding(
            padding: const EdgeInsets.all(8.0),
            child: Image.asset(imge),
          ),
        ),
        SizedBox(height: 5),
        Text(type.name)
      ],
    );
  }
}
*/